---
import BaseTail from '@server/BaseTail.astro';
import BottomBar from '@server/BottomBar.astro';
import AppFooter from '@server/app/AppFooter.astro';
import AppBar from 'src/components/server/AppBar.astro';
import AppRail from 'src/components/server/AppRail.astro';
import BaseHead from '../components/server/BaseHead/BaseHead.astro';

type Props = {
  title?: string;
  description?: string;
  image?: string;
  noun?: string;
  shortTitle?: string;
};

const { title, description, image, noun, shortTitle } = Astro.props;
---

<html lang="en">
  <BaseHead title={title} description={description} image={image} />
  <body>
    <AppBar title={title} noun={noun} shortTitle={shortTitle}>
      <slot name="app-bar" />
    </AppBar>
	  <AppRail title={title}/>
    <BottomBar />

    <cn-tray-button></cn-tray-button> 
    <nav class="cn-tray">
      <slot name="app-tray" />
   </nav>
   
	  <main>
      <div class="view-patch">
      <slot />
      </div>
      <AppFooter />
    </main>
    <nav id="fab-tray"><slot name="fab-tray"></slot></nav>
    <slot name="app-background-poster" />
    <BaseTail />
  </body>
</html>

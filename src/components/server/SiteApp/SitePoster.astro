---
import type { Site } from '@schemas/SiteSchema';
import BackgroundPoster from '@server/ui/BackgroundPoster.astro';

interface Props {
  site: Site;
}
const { site } = Astro.props;
---
{ site.backgroundURL && 
  <BackgroundPoster src={site.backgroundURL} />
}
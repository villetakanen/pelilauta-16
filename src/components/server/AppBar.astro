---
interface Props {
  title?: string;
  tray?: boolean;
  menu?: boolean;
}

const { title } = Astro.props;
const barTitle = title || 'Pelilauta 2';
---

<header class="cn-app-bar">
  <div class="titleContainer"><h3>{barTitle}</h3></div>
  <div style="flex: 1"></div>
  <slot />
  <cn-share-button></cn-share-button>
</header>

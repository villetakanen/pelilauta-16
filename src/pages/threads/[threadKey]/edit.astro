---

import { ThreadEditorApp } from '@client/threads/ThreadEditorApp/ThreadEditorApp';
import ModalPage from '@layouts/ModalPage.astro';

const { threadKey } = Astro.params;

if (!threadKey) {
  throw new Error('threadKey is required');
}
---
<ModalPage>
  <ThreadEditorApp threadKey={threadKey} client:only="solid-js"/>
</ModalPage>
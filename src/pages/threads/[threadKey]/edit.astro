---

import { ThreadEditor } from '@client/ThreadApp/ThreadEditor/ThreadEditor';
import ModalPage from '@layouts/ModalPage.astro';

const { threadKey } = Astro.params;

if (!threadKey) {
  throw new Error('threadKey is required');
}
---
<ModalPage>
  <ThreadEditor threadKey={threadKey} client:only="solid-js"/>
</ModalPage>
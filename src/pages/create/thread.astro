---
import { ThreadEditor } from '@client/ThreadApp/ThreadEditor/ThreadEditor';
import ModalPage from '@layouts/ModalPage.astro';
import { t } from '@utils/i18n';

const title = `${t('app:shortname')} – ${t('actions:create.thread')}`;
const channel = Astro.url.searchParams.get('channel') || undefined;
---

<ModalPage title={title}>
 <ThreadEditor
   topic={channel}
   client:only="solid-js"/>
</ModalPage>
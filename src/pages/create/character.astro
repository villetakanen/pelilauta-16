---
import ModalPage from '@layouts/ModalPage.astro';
import CreateCharacterForm from '@svelte/characters/CreateCharacterForm.svelte';
import { t } from '@utils/i18n';

const title = `${t('app:shortname')} – ${t('characters:create.title')}`;
const shortTitle = t('site:create.title');
---

<ModalPage title={title} shortTitle={shortTitle}>
  <CreateCharacterForm client:only="svelte" />
</ModalPage>